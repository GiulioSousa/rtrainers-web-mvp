import { Component } from '@angular/core';

// Placeholder. Ver login.ts.
// Classe prefixada com Pagina para nao colidir com a interface Agenda do model.
@Component({
  selector: 'app-agenda',
  template: '<p>Agenda</p>'
})
export class PaginaAgenda {}
