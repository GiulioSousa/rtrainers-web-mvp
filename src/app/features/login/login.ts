import { Component } from '@angular/core';

// Placeholder. Template inline de proposito: vira arquivo proprio quando a
// tela for construida de verdade.
@Component({
  selector: 'app-login',
  template: '<p>Login</p>'
})
export class PaginaLogin {}
